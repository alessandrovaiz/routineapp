import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:routineapp/app/modules/login/tabs/tab_page_login.dart';

void main() {
  testWidgets('TabsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TabsPage(title: 'Tabs')));
    //  final titleFinder = find.text('Tabs');
    //  expect(titleFinder, findsOneWidget);
  });
}
